<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ToDo List</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
        }

        h1 {
            background-color: #007bff;
            color: white;
            padding: 10px;
            text-align: center;
        }

        ul {
            list-style-type: none;
            padding: 0;
        }

        li {
            background-color: #ffffff;
            border: 1px solid #dddddd;
            margin: 5px 0;
            padding: 10px;
            display: flex;
            justify-content: space-between;
        }

        form {
            margin-top: 10px;
            display: flex;
            justify-content: center;
        }

        input[type="text"] {
            padding: 5px;
            width: 70%;
            border: 1px solid #ccc;
        }

        button {
            padding: 5px 10px;
            background-color: #007bff;
            color: white;
            border: none;
            cursor: pointer;
        }

        button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <h1><%= kindOFDay %></h1>
    
    <ul>
        <li>Buy Food</li>
        <li>Cook Food</li>
        <li>Eat Food</li>
        <% for (let i = 0; i < newListItems.length; i++) { %>
            <li><%= newListItems[i] %></li>
        <% } %>
        <form action="/" method="post">
            <input type="text" name="newItem" placeholder="enter new newItem">
            <button type="submit">Add</button>
        </form>
    </ul>
</body>
</html>
